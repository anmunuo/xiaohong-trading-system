# 2026-06-01 系统全面审计要点

> 来源：小红交易系统 v8.0+ 全面审计，3 个子代理并行扫描

## 性能瓶颈（已修复）

| 瓶颈 | 修复前 | 修复后 | 提升 |
|:--|:--|:--|:--|
| `get_stock_realtime` 逐只 subprocess | 50只 × 30s = 25min | Sina 批量 0.03s | 5000x |
| `stock_recommender` 三重查询 | 每层独立拉取 | run() 预取缓存 | 3x |
| 整体选股流程 | ~25min | <5s | 300x |

## 进化引擎覆盖（修复后：37→49 参数）

| 模块 | 修复前 | 修复后 | 新增 |
|:--|:--|:--|:--|
| 推荐引擎 | 3 | 8 | 五因子权重 |
| 弹药库 | 5 | 9 | R值分母/止盈缓冲/集中度/流动性 |
| 狙击手 | 11 | 12 | P0确认tick |
| 基础设施 | 2 | 4 | 行情缓存TTL/MA刷新间隔 |

## 代码健壮性（已修复）

- 19 处裸 `except:` → `except Exception:`（knowledge_base/resource_pool/data_pipeline）
- strategy_bridge R值 fallback 6927 → 0
- strategy_bridge 仓位 33.3% 硬编码 → 动态读取 `maxPositionPerStock`

## 弹药库 v4.1 修复清单

| # | 问题 | 修复 |
|:--|:--|:--|
| P0-1 | cron 无 --update | `cron_ammo.sh` 改为 `ammo_risk.py --update` |
| P0-2 | 双重净值字段 | 统一到 `accountInfo.currentNetValue` |
| P1-3 | R值未自动计算 | `calc_r_value()` 每次 --update 写入 |
| P1-4 | 流动性用当日量 | `_get_avg_amount_5d()` 5日均量 |
| P1-5 | 行业分类靠名称 | tushare `stock_basic.industry` 优先 |
| P1-6 | 止盈无缓冲 | 止损距现价 ≥3% 动态缓冲 |
| P2-7 | update 无池交叉 | `inPool`/`poolCheckedAt` 标记 |
| P2-8 | 周趋势正则解析 | `netValueHistory[]` 数组 |
| P2-9 | 重复拉行情 | `get_cached_quotes()` 120s缓存 |

## 狙击手 v4.0 架构

- 从 cron 每30分钟 → systemd 守护进程每3秒
- Sina 批量 HTTP 单请求查全部持仓
- 状态机去重：仅优先级跃迁告警
- P0 需 2 tick 确认（防数据尖刺）
- 存活检测 cron 每5分钟自动恢复
